# c++ algorithms

```cpp
vector <int> vec;

sort(vec.begin(), vec.end())

// copy vec into list, dont copy adjacent equal elements
unique_copy(vec.begin(), vec.end(), list.begin()) 

//back_inserter
unique_copy(vec.begin(), vec.end(), back_inserter(res))

//
```

